# Exemplos

Inclua aqui apenas XMLs ficticios ou anonimizados.

Para validar rapidamente:

```bash
dotnet run --project Dacte.CTe.Core.Sample/Dacte.CTe.Core.Sample.csproj -- docs/exemplos out
```

Nao publique XMLs reais sem anonimizar CNPJ, razao social, enderecos, chaves de acesso, protocolos e assinaturas.
